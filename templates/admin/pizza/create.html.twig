{% extends "base.html.twig" %}

{% block title %}Ajout d'une nouvelle pizza{% endblock %}

{% block body %}
    <h2>{% block page_title 'Nouvelle pizza' %}</h2>

	<p><a href="{{ path('app_admin_pizza_list') }}">Retour à la liste</a></p>

	<div class="container-form">

        {{form_start(form)}}

            <div class="container-form-info">

                <div class="form-group">
                    {{form_row(form.name)}}
                </div>
                <div class="form-group">
                    {{form_row(form.description)}}
                </div>
                <div class="form-group">
                    {{form_row(form.price)}}
                </div>
                <div class="form-group">
                    {# {{form_row(form.password)}} #}
                </div>
                <div class="form-group">
                    {{form_row(form.picture)}}
                </div>
            </div>

        {{form_end(form)}}
    </div>

{% endblock %}